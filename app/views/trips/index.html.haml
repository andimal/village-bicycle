-# = @trip.inspect

-# = @points
-# = Trip.first.set_directions_points

%script
  - (0..23).each do |hour|
    - if hour < 10
      - hour = "0#{hour}"
  
    = render file: "trips/static-data/trip-data-#{hour}"

  / var trip_data = [
  / - @trips.each do |trip|
  /   - if trip.directions_points.present?
  /     != "'#{trip.directions_points}',"
  / ];
-# = render file: "trips/hourly_trips/hour_11"
-# = render file: "trips/hourly_trips/hour_0"

%section.map-container
  #map-day.map.map-day
  #map-night.map.map-night

  #map-00.map.heatmap.map-00.current-heatmap
  - (1..23).each do |hour|
    - if hour < 10
      - hour = "0#{hour}"
    .map.heatmap{id: "map-#{hour}", class: "map-#{hour}"}

  .map-slider{data: {hour: @current_hour}}

